import axios from 'axios'
import { defineStore } from 'pinia'

export const useTooltip = defineStore('Tooltip', {
    state: () => ({
        show: false,
        params: {} as any,
    }),
    getters: {

    },
    actions: {

    },

})
